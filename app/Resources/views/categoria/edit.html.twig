{% extends 'base.html.twig' %}

{% block body %}
    <h3>Editar Categoria</h3>
    <div class="content80">
        {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input class="btn btn-default pull-right" type="submit" value="Aceptar" />
        {{ form_end(edit_form) }}
        <br>
        <ul class="ul-ls-none list-inline">
            <li>
                <a class="btn btn-default" href="{{ path('admin_categoria_index') }}">Volver a la lista</a>
            </li>
            <li>
                {{ form_start(delete_form) }}
                    <button class="btn btn-danger" type="submit"><i class="fa fa-ban"></i> Eliminar Categoría</button>
                {{ form_end(delete_form) }}
            </li>
        </ul>
    </div>
{% endblock %}
